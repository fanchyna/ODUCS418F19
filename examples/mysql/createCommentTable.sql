CREATE TABLE COMMENT ( CommentId Int NOT NULL PRIMARY KEY AUTO_INCREMENT, UserId varchar(50) NOT NULL, Comment text, CONSTRAINT COMMENT_FK FOREIGN KEY(UserId) REFERENCES userprofile (userid) ON UPDATE CASCADE );
